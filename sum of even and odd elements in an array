Write a program to sum of even and odd elements in an array.

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

   int a[10],n,so=0,se=0,i;
    scanf("%d",&n);
    
    for(i=0;i<n;i++){
    scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++){
        if(a[i]%2==0)
        {
            se=se+a[i];
        }
        else
        {
            so=so+a[i];
        }
    }
    printf("%d\n",so);
    printf("%d",se);
    
    return 0;
}
